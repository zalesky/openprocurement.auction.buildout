[buildout]
extends =
    sources.cfg

parts +=
    npm-install-packages

[auctions.ini]
debug = true
auto_build =

[npm-install-packages]
recipe = plone.recipe.command
command = cd ${buildout:directory}/src/openprocurement.auction/openprocurement/auction && npm install && ./node_modules/.bin/bower install -y
